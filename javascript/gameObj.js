var gameObj = {
/* Constructor
 State - (p)laying, (w)on, (l)ost
 */
function gameObj(scientist, state, game) {
  this.scientist = scientist;
  this.state = state;
  this.game = game;
}

function startNewGame(){
// reset number of wrong guesses
// clear details display
// Display new name
}

function playGame(key){
//check if key matches any letter in the name
//update guess list
//check if # of max wrong guesses have been reached
}

function endGame(){
// Update win or loss Count 
// Display details about scientist
 }
//Closing  gameObj
}

/*********************************************
*********************************************/
var scientist = {
	function scientist
}
		/*make: "Honda",
		model: "Fit",
		color: "Blue Rasberry",
		mileage: 3000,
		isWorking: true,
		test: this.mileage*2,

		driveToWork: function(){
			debugger;
			alert("Old Mileage: " + this.mileage)
			
			this.mileage = this.mileage + 8;
			
			alert("New mileage: " + this.mileage);
		},

		driveAroundWorld: function(){

			alert("Old Mileage: " + this.mileage);

			this.mileage = this.mileage + 24000;

			alert("New Mileage: " + this.mileage);
			alert("Car needs a tuneup!")

			this.isWorking = false;
		},

		getTuneUp: function(){
			alert("Car is ready to go!");
			this.isWorking = true;
		},

		honk: function(){
			alert("Meeeeeeeeowwwww")
		} 
	}

*/